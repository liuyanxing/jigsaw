{
  "name": "jigsaw-prototype",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "矩形块拼图 Web 原型(为 Android 重写而设计:core 纯逻辑可直译 Kotlin)",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
